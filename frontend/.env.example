# Copia a .env y pega tu token (cp .env.example .env). No versiones .env.
#
# https://account.mapbox.com/access-tokens/ — el token "Default public token"
# sirve; no requiere tarjeta para el free tier (50k cargas de mapa/mes).
VITE_MAPBOX_TOKEN=
